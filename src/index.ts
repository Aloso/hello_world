// Import the polyfills you need! Most are in 'core-js/features/'
// These polyfills are for promises, generators and async-await:
import 'core-js/features/promise'
import 'regenerator-runtime/runtime'

// If you don't want to import

// Import stylesheets!
// NOTE: They are extracted to a CSS file during the build process.
import 'normalize.css/normalize.css'
import './index.sass'

// TODO: This is the entry point of your application!
console.log('Hello world!')

// You can import other modules, either statically or dynamically.
// Use the ES6 syntax: https://webpack.js.org/api/module-methods/#es6-recommended
